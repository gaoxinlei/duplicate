测试两个分支创建同名文件的git log
说明:
写这个简单示例是因为实际项目中碰到了追责,不知道从哪个版本起一个同名函数形参个数发生了变化.
由于自己之前遇到过类似情况,即从同一个分支切出来的多个分支,如果我们建立了同名的文件,先后合
回主分支时,可以直接覆盖掉,这样git log虽然会显示多个记录,但git log -p只会有一个历史.
以此备忘.
简单示例.
先初始化工程,建立read文件.
建立并切到dev1分支,创建Test并写下两个形参的函数,提交.
切回master
建立并切到dev2分支,创建Test并写下三个形参的函数,提交.
切回master,把dev1合并
把dev2合并,会要求解决冲突,删除dev1上的操作而保留dev2的操作.模拟两个人冲突,一人选择只保留自己或对方的代码情况.
解决好冲突后git add git commit
查看git log,有三个历史.
查看git log -p Test.java 只有一个历史,找不到形参为2个参数的.
